let str1 = prompt("Enter the first string:");
let str2 = prompt("Enter the second string:");

if (str1.length === str2.length) {
    alert("Both strings have the same length.");
} else {
    alert("Strings have different lengths.");
}
